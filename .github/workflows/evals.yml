# name: Agentic Quality Evaluations & Regression Gate
# 
# on:
#   pull_request:
#     paths:
#       - 'backend-agentic/**'
#       - '.github/workflows/evals.yml'
#   push:
#     branches:
#       - main
#     paths:
#       - 'backend-agentic/**'
#       - '.github/workflows/evals.yml'
#   workflow_dispatch:
# 
# jobs:
#   evaluations:
#     name: Golden Benchmark Evaluation & Regression Gate
#     runs-on: ubuntu-latest
#     timeout-minutes: 15
# 
#     defaults:
#       run:
#         working-directory: backend-agentic
# 
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4
# 
#       - name: Setup Node.js 20.x
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20.x'
#           cache: 'npm'
#           cache-dependency-path: backend-agentic/package-lock.json
# 
#       - name: Install Dependencies
#         run: npm ci
# 
#       - name: TypeScript Build Check
#         run: npm run build
# 
#       - name: Run Unit & Integration Test Suites
#         run: npm run test
#         env:
#           NODE_ENV: test
# 
#       - name: Run Golden Benchmark Evaluation Gate (20 Syllabi)
#         run: npm run test:evals
#         env:
#           NODE_ENV: test
#           OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
#           OPENCODE_BASE_URL: ${{ secrets.OPENCODE_BASE_URL || 'https://api.opencode.ai/v1' }}
#           LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
#           LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
#           LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST || 'https://cloud.langfuse.com' }}
